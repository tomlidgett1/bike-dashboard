/** Shared horizontal padding for dashboard topbar + page content. */
export const dashboardHorizontalPadding = "px-4 md:px-5";

/** Topbar uses the same rhythm as full-bleed store pages (e.g. Nest). */
export const dashboardTopbarPadding = dashboardHorizontalPadding;
