-- Placeholder for migration version 20260612021612, which was applied
-- directly to the remote project (it exists in supabase_migrations.schema_migrations
-- but had no file in this repo). Added so `supabase db push` can reconcile
-- migration history. Intentionally a no-op.
SELECT 1;
